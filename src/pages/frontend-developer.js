import React from "react"
import CareerPathPage from "../layouts/careerPath"

export default ({ location }) => (
  <div>
    <CareerPathPage slug="/frontend-developer" location={location} />
  </div>
)
