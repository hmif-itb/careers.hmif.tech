import React from "react"
import CareerPathPage from "../layouts/careerPath"

export default ({ location }) => (
  <div>
    <CareerPathPage slug="/mobile-developer" location={location} />
  </div>
)
