import React from "react"
import CareerPathPage from "../../layouts/careerPath"

const DataAnalyst = ({ location }) => (
  <div>
    <CareerPathPage slug="/data-analyst" location={location} />
  </div>
)
export default DataAnalyst
