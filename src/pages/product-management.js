import React from "react"
import CareerPathPage from "../layouts/careerPath"

export default ({ location }) => (
  <div>
    <CareerPathPage slug="/product-management" location={location} />
  </div>
)
